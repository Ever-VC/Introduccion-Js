const numero1 = 20;
const numero2 = 20;

console.log(parseInt(numero1)); // ParseInt es una función que convierte un string a un número entero
console.log(numero1.toString()); // toString es un método que convierte un número a un string

// La diferencia entre una función y un método es que la función se invoca de la 
//siguiente manera: nombreFuncion(parametro) y el método se invoca de la 
//siguiente manera: nombreVariable.nombreMetodo(parametro)